name: esteira-publica
on:
  push:
    branches: [master]
    paths: ["queue/**"]
  workflow_dispatch: {}
jobs:
  publicar:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: achar fila pendente
        id: fila
        run: |
          set -e
          PEND=""
          for d in queue/*/; do
            [ -f "$d/meta.json" ] || continue
            if ! grep -q '"published": true' "$d/meta.json"; then PEND="$d"; fi
          done
          echo "dir=$PEND" >> "$GITHUB_OUTPUT"
          echo "fila pendente: $PEND"
      - name: publicar no Publer
        if: steps.fila.outputs.dir != ''
        env:
          PUBLER_KEY: ${{ secrets.PUBLER_KEY }}
        run: |
          python pipeline/publish_queue.py "${{ steps.fila.outputs.dir }}"
      - name: marcar publicado
        if: steps.fila.outputs.dir != ''
        run: |
          git config user.name "esteira-bot"
          git config user.email "esteira@users.noreply.github.com"
          git add -A
          git diff --cached --quiet || git commit -m "esteira: marcado publicado [skip ci]"
          git push
